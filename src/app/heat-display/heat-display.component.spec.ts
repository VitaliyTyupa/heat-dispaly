import { HeatDisplayComponent } from './heat-display.component';

describe('HeatDisplayComponent', () => {
  let component: HeatDisplayComponent;
  beforeEach(() => {
   component = new HeatDisplayComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
